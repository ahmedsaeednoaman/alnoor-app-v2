import type { Metadata } from "next";import { redirect } from "next/navigation";import { OperationForm } from "@/components/operations/operation-form";import { getCurrentSession } from "@/lib/auth/session";
export const metadata:Metadata={title:"إضافة شغل"};export default async function Page({searchParams}:{searchParams:Promise<{edit?:string}>}){const auth=await getCurrentSession();if(!auth)redirect("/login");if(!auth.user.permissions.includes("operations.create"))redirect("/");const {edit}=await searchParams;if(edit)redirect(`/operations/${edit}/edit`);return <OperationForm canManageCatalogs={auth.user.permissions.includes("catalogs.manage")} canManageWorkForms={auth.user.permissions.includes("settings.work_forms.manage")}/>}
