import { NextResponse } from "next/server";import { requirePermission } from "@/lib/auth/guards";import { apiError,operationError,requestId } from "@/lib/operations/api";import { cancelOperation } from "@/lib/operations/service";import { cancelSchema } from "@/lib/operations/validation";
export async function POST(request:Request,{params}:{params:Promise<{operationId:string}>}){const id=requestId();try{const auth=await requirePermission("operations.cancel");const parsed=cancelSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return apiError(400,"VALIDATION_ERROR","سبب الإلغاء مطلوب.",id,parsed.error.flatten());await cancelOperation((await params).operationId,parsed.data.reason,auth.user);return NextResponse.json({ok:true});}catch(e){return operationError(e,id)}}
